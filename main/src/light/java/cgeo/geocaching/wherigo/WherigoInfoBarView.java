package cgeo.geocaching.wherigo;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.RelativeLayout;

/** WherigoInfoBarView stub for light build — Wherigo not available. */
public class WherigoInfoBarView extends RelativeLayout {

    public WherigoInfoBarView(final Context context) {
        super(context);
    }

    public WherigoInfoBarView(final Context context, final AttributeSet attrs) {
        super(context, attrs);
    }

    public WherigoInfoBarView(final Context context, final AttributeSet attrs, final int defStyle) {
        super(context, attrs, defStyle);
    }
}
